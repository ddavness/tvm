#pragma once
#include "win32-dll.h"

#include <string>

#define TVM_VERSION_MAJOR @tvm_VERSION_MAJOR@
#define TVM_VERSION_MINOR @tvm_VERSION_MINOR@
#define TVM_VERSION_PATCH @tvm_VERSION_PATCH@

namespace tvm
{
    PUB int16_t version_major();
    PUB int16_t version_minor();
    PUB int16_t version_patch();
    PUB std::string version();
} // namespace tvm
